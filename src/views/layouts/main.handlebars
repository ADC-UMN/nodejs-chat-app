<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<title>ADC Chat App</title>
		<!-- Using Bootstrap 5 for styling, makes things much easier :) -->
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
			rel="stylesheet"
			integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
			crossorigin="anonymous"
		/>

		<style>
			.container {
				width: 100%;
				margin-right: auto;
				margin-left: auto;
			}

			@media (min-width: 768px) {
				.container {
					max-width: 720px;
				}
			}
		</style>
	</head>

	<body>
		<!-- A small padding div so the top of the page isn't *actually* at the top of the page -->
		<div style="height: 44px"></div>
		<!-- This is where our page's content is rendered. -->
		<div class="container">{{{body}}}</div>
	</body>
</html>
